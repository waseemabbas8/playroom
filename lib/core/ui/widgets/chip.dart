import 'package:flutter/material.dart';

class RoundedChip extends StatelessWidget {
  const RoundedChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
